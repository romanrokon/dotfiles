# Mac-only env additions. Currently unused: brew eval and $HOME/.bin already
# handled in env.zsh. Kept as placeholder for future Mac-only PATH/env tweaks.
# Note: this file is not sourced by .zshrc — wire it in if/when needed.
